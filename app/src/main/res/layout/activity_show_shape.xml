<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:layout_width="300dp"
        android:layout_height="300dp"
        app:srcCompat="?android:attr/textCheckMarkInverse"
        android:layout_centerInParent="true"
        android:id="@+id/shape_image_view"
        android:layout_alignTop="@+id/shape_image_view"
        android:layout_alignStart="@+id/shape_image_view" />

    <Button
        android:id="@+id/restart_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/shape_image_view"
        android:text="@string/restart_button"
        />
</RelativeLayout>